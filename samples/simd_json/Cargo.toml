[package]
name = "simd_json_sample"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
simd-json = "0.17.0"

[profile.release]
opt-level = 1
lto = "fat"
codegen-units = 1
panic = "abort"
