// @generated by Rustlyn.Bindings. Do not edit by hand.
#nullable enable
using System.Management.Automation;
using Rustlyn.PowerShellSupport;

namespace Rustlyn.PowerShellCmdlets;

[Cmdlet(VerbsData.ConvertTo, "RustJson")]
[OutputType(typeof(string))]
public sealed class ConvertToRustJsonCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public int Depth { get; set; } = 2;

    [Parameter]
    public SwitchParameter Compress { get; set; }

    [Parameter]
    public SwitchParameter EnumsAsStrings { get; set; }

    public ConvertToRustJsonCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_json_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_json_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_json_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustJson")]
[OutputType(typeof(System.Management.Automation.PSObject), typeof(System.Collections.Generic.IDictionary<string, object>))]
public sealed class ConvertFromRustJsonCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public SwitchParameter AsHashtable { get; set; }

    [Parameter]
    public SwitchParameter NoEnumerate { get; set; }

    public ConvertFromRustJsonCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_json_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_json_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_json_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustCsv")]
[OutputType(typeof(string))]
public sealed class ConvertToRustCsvCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public char Delimiter { get; set; }

    [Parameter]
    public SwitchParameter UseCulture { get; set; }

    [Parameter]
    public SwitchParameter NoTypeInformation { get; set; }

    [Parameter]
    public SwitchParameter IncludeTypeInformation { get; set; }

    [Parameter]
    public SwitchParameter NoHeader { get; set; }

    [Parameter]
    public string[]? QuoteFields { get; set; }

    [Parameter]
    [ValidateSet("Always", "AsNeeded", "Never")]
    public string? UseQuotes { get; set; }

    public ConvertToRustCsvCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_csv_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_csv_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_csv_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustCsv")]
[OutputType(typeof(System.Management.Automation.PSObject))]
public sealed class ConvertFromRustCsvCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    [AllowEmptyString]
    public string[]? InputObject { get; set; }

    [Parameter]
    public char Delimiter { get; set; }

    [Parameter]
    public SwitchParameter UseCulture { get; set; }

    [Parameter]
    public string[]? Header { get; set; }

    public ConvertFromRustCsvCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_csv_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_csv_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_csv_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustToml")]
[OutputType(typeof(string))]
public sealed class ConvertToRustTomlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public int Depth { get; set; } = 8;

    public ConvertToRustTomlCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_toml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_toml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_toml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustToml")]
[OutputType(typeof(System.Management.Automation.PSObject), typeof(System.Collections.Generic.IDictionary<string, object>))]
public sealed class ConvertFromRustTomlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    public ConvertFromRustTomlCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_toml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_toml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_toml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustXml")]
[OutputType(typeof(string), typeof(System.Xml.XmlDocument), typeof(System.IO.Stream))]
public sealed class ConvertToRustXmlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public int Depth { get; set; } = 2;

    [Parameter]
    public SwitchParameter NoTypeInformation { get; set; }

    [Parameter]
    [ValidateSet("String", "Document", "Stream")]
    public string As { get; set; } = "Document";

    public ConvertToRustXmlCommand()
    {
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_xml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_xml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var captureXmlStream = string.Equals(As, "Stream", StringComparison.OrdinalIgnoreCase) && methodName.EndsWith("_end_processing", StringComparison.Ordinal);
        var boundParameters = captureXmlStream
            ? new System.Collections.Generic.Dictionary<string, object?>(MyInvocation.BoundParameters, StringComparer.OrdinalIgnoreCase) { ["As"] = "String" }
            : MyInvocation.BoundParameters;
        var context = new PowerShellCmdletContext(
            value =>
            {
                if (!captureXmlStream)
                {
                    WriteObject(value);
                }
                else
                {
                    var xml = value?.ToString() ?? string.Empty;
                    WriteObject(new System.IO.MemoryStream(System.Text.Encoding.UTF8.GetBytes(xml)), enumerateCollection: false);
                }
            },
            (value, enumerateCollection) =>
            {
                if (!captureXmlStream)
                {
                    WriteObject(value, enumerateCollection);
                }
                else
                {
                    var xml = value?.ToString() ?? string.Empty;
                    WriteObject(new System.IO.MemoryStream(System.Text.Encoding.UTF8.GetBytes(xml)), enumerateCollection: false);
                }
            },
            _writeVerboseCallback,
            _writeWarningCallback,
            _writeErrorStringCallback,
            _writeErrorCallback,
            _throwTerminatingErrorCallback,
            _shouldProcessCallback,
            _shouldProcessWithActionCallback,
            boundParameters,
            inputObject,
            _cancellation,
            EnsureLifecycleStateHandle());
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_xml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustXml")]
[OutputType(typeof(System.Xml.XmlDocument))]
public sealed class ConvertFromRustXmlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    public ConvertFromRustXmlCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_xml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_xml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_xml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustYaml")]
[OutputType(typeof(string))]
public sealed class ConvertToRustYamlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    public ConvertToRustYamlCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_yaml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_yaml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_yaml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustYaml")]
[OutputType(typeof(System.Management.Automation.PSObject), typeof(System.Collections.Generic.IDictionary<string, object>))]
public sealed class ConvertFromRustYamlCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public SwitchParameter AsHashtable { get; set; }

    [Parameter]
    public SwitchParameter NoEnumerate { get; set; }

    public ConvertFromRustYamlCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_yaml_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_yaml_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_yaml_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustBson")]
[OutputType(typeof(byte[]))]
public sealed class ConvertToRustBsonCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public int Depth { get; set; } = 8;

    public ConvertToRustBsonCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_bson_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_bson_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_bson_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustBson")]
[OutputType(typeof(System.Management.Automation.PSObject), typeof(System.Collections.Generic.IDictionary<string, object>))]
public sealed class ConvertFromRustBsonCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public SwitchParameter AsHashtable { get; set; }

    [Parameter]
    public SwitchParameter NoEnumerate { get; set; }

    public ConvertFromRustBsonCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_bson_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_bson_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_bson_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertTo, "RustCbor")]
[OutputType(typeof(byte[]))]
public sealed class ConvertToRustCborCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public int Depth { get; set; } = 8;

    public ConvertToRustCborCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_cbor_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_cbor_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_to_rust_cbor_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

[Cmdlet(VerbsData.ConvertFrom, "RustCbor")]
[OutputType(typeof(System.Management.Automation.PSObject), typeof(System.Collections.Generic.IDictionary<string, object>))]
public sealed class ConvertFromRustCborCommand : PSCmdlet
{
    private readonly PowerShellCmdletCancellation _cancellation = new();
    private PowerShellCmdletContext? _cachedContext;
    private readonly Action<object?> _writeObjectCallback;
    private readonly Action<object?, bool> _writeObjectEnumeratedCallback;
    private readonly Action<string> _writeVerboseCallback;
    private readonly Action<string> _writeWarningCallback;
    private readonly Action<string> _writeErrorStringCallback;
    private readonly Action<ErrorRecord> _writeErrorCallback;
    private readonly Action<ErrorRecord> _throwTerminatingErrorCallback;
    private readonly Func<string, bool> _shouldProcessCallback;
    private readonly Func<string, string?, bool> _shouldProcessWithActionCallback;
    private int _lifecycleStateHandle;

    [Parameter(Position = 0, ValueFromPipeline = true, Mandatory = true)]
    public object? InputObject { get; set; }

    [Parameter]
    public SwitchParameter AsHashtable { get; set; }

    [Parameter]
    public SwitchParameter NoEnumerate { get; set; }

    public ConvertFromRustCborCommand()
    {
        _writeObjectCallback = HandleWriteObject;
        _writeObjectEnumeratedCallback = HandleWriteObjectEnumerated;
        _writeVerboseCallback = WriteVerbose;
        _writeWarningCallback = WriteWarning;
        _writeErrorStringCallback = HandleWriteErrorString;
        _writeErrorCallback = WriteError;
        _throwTerminatingErrorCallback = ThrowTerminatingError;
        _shouldProcessCallback = HandleShouldProcess;
        _shouldProcessWithActionCallback = HandleShouldProcessWithAction;
    }

    private void HandleWriteObject(object? value)
        => WriteObject(value);

    private void HandleWriteObjectEnumerated(object? value, bool enumerateCollection)
        => WriteObject(value, enumerateCollection);

    private void HandleWriteErrorString(string message)
        => WriteError(CreateErrorRecord(message));

    private bool HandleShouldProcess(string target)
        => ShouldProcess(target);

    private bool HandleShouldProcessWithAction(string target, string? action)
        => ShouldProcess(target, action);

    private PowerShellCmdletContext GetOrCreateContext(object? inputObject)
    {
        var context = _cachedContext;
        if (context is null)
        {
            context = new PowerShellCmdletContext(
                _writeObjectCallback,
                _writeObjectEnumeratedCallback,
                _writeVerboseCallback,
                _writeWarningCallback,
                _writeErrorStringCallback,
                _writeErrorCallback,
                _throwTerminatingErrorCallback,
                _shouldProcessCallback,
                _shouldProcessWithActionCallback,
                MyInvocation.BoundParameters,
                inputObject,
                _cancellation,
                EnsureLifecycleStateHandle());
            _cachedContext = context;
            return context;
        }

        context.SetInputObject(inputObject);
        return context;
    }

    protected override void ProcessRecord()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_cbor_process_record", InputObject);
        }
        catch
        {
            ReleaseLifecycleState();
            throw;
        }
    }

    protected override void EndProcessing()
    {
        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_cbor_end_processing", null);
        }
        finally
        {
            ReleaseLifecycleState();
        }
    }

    protected override void StopProcessing()
        => _cancellation.RequestCancellation();

    private void InvokeRustEntrypoint(string engineAssemblyName, string typeName, string methodName, object? inputObject, bool checkCancellation = true)
    {
        var context = GetOrCreateContext(inputObject);
        PowerShellGeneratedCmdletInvoker.InvokeLifecycle(engineAssemblyName, typeName, methodName, context, checkCancellation);
    }

    private int EnsureLifecycleStateHandle()
    {
        if (_lifecycleStateHandle == 0)
        {
            _lifecycleStateHandle = PowerShellGeneratedCmdletInvoker.CreateLifecycleStateHandle();
        }

        return _lifecycleStateHandle;
    }

    private void ReleaseLifecycleState()
    {
        var lifecycleStateHandle = _lifecycleStateHandle;
        if (lifecycleStateHandle == 0)
        {
            return;
        }

        try
        {
            InvokeRustEntrypoint("rustlyn_powershell_format_cmdlets.dll", "Rustlyn.GeneratedModule", "convert_from_rust_cbor_cleanup", null, checkCancellation: false);
        }
        finally
        {
            _lifecycleStateHandle = 0;
            PowerShellGeneratedCmdletInvoker.ReleaseLifecycleStateHandle(lifecycleStateHandle);
        }
    }

    private static ErrorRecord CreateErrorRecord(string message)
        => new(new RuntimeException(message), "RustlynPowerShellCmdletError", ErrorCategory.NotSpecified, null);
}

